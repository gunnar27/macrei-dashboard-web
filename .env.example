# Base URL of the autonomous-pm dashboard read-API (FastAPI).
# Local dev: the backend running on port 8090.
# Production: your deployed backend URL (Railway/Fly), e.g. https://api.macrei.app
NEXT_PUBLIC_API_BASE_URL=http://localhost:8090
